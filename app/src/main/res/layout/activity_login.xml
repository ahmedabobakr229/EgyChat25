<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_gravity="center"
    android:gravity="center"
    android:padding="20dp"
    android:background="#68e696"
    tools:context=".Auth_System.Login">

    <include layout="@layout/app_bar_layout"
        android:id="@+id/login_app_bar"></include>

    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:id="@+id/lgn_app_logo"
        android:layout_marginBottom="20dp"
        android:foregroundGravity="center"
        android:src="@drawable/app"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/edittext_backgrounds"
        android:id="@+id/lgn_name"
        android:hint="Name"
        android:textAlignment="center"/>
    <EditText
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/edittext_backgrounds"
        android:id="@+id/lgn_pass"
        android:hint="Password"
        android:inputType="textPassword"
        android:textAlignment="center"/>
    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Login"
        android:layout_marginTop="10dp"
        android:textSize="17dp"
        android:textAllCaps="false"
        android:layout_gravity="center"
        android:id="@+id/lgn_login"
        android:background="@drawable/buttonbackground"/>
</LinearLayout>